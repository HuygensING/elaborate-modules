if (entry.facsimiles)
	header
		each facsimile, n in entry.facsimiles
			if facsimile.zoom===zoomUrl
				h3= 'Facsimile ' + (n+1) + ' of ' + Object.keys(entry.facsimiles).length

		//- select
		//- 	each facsimile, n in entry.facsimiles
		//- 		option(value=facsimile.zoom, selected=facsimile.zoom===zoomUrl)= 'Facsimile ' + (n+1) + ' of ' + Object.keys(entry.facsimiles).length

	iframe(src=entry.facsimiles[0].zoom, border="0", width="100%")